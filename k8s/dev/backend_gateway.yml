apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kubernetes.io/change-cause: "Image feralexis007/manantial-gateway:dev-ce32579"
  name: backend-gateway-deployment-dev
  labels:
    app: back-gateway-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: back-gateway-dev
  template:
    metadata:
      labels:
        app: back-gateway-dev
    spec:
      containers:
        - name: gateway-service
          image: feralexis007/manantial-gateway:dev-ce32579
          imagePullPolicy: Always
          securityContext:
            runAsUser: 10003
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 8090
          env:
            - name: AUTH_SERVER
              valueFrom:
                configMapKeyRef:
                  name: back-gateway-dev
                  key: auth_server
            - name: CUSTOMER_URL
              valueFrom:
                configMapKeyRef:
                  name: back-gateway-dev
                  key: customers_url
            - name: CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: gateway-secrets-dev
                  key: keycloak_client_id
            - name: CUSTOMER_BASEURL
              valueFrom:
                configMapKeyRef:
                  name: back-gateway-dev
                  key: customer_baseurl
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health   # <-- Asegúrate de que este endpoint existe en tu app
              port: 8090
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8090
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            successThreshold: 1
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: backend-gateway-service-dev
spec:
  type: ClusterIP
  selector:
    app: back-gateway-dev
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 8090
