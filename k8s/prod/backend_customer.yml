apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kubernetes.io/change-cause: "Image feralexis007/manantial-customers-service:prod-d8fee31"
  name: backend-customer-deployment-prod
  labels:
    app: back-customer-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: back-customer-prod
  template:
    metadata:
      labels:
        app: back-customer-prod
    spec:
      containers:
        - name: customers-service
          image: feralexis007/manantial-customers-service:prod-d8fee31
          ports:
            - containerPort: 8081
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: back-customer-prod
                  key: db_host
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mongo-secrets-prod
                  key: mongo_user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mongo-secrets-prod
                  key: mongo_password
            - name: KAFKA_HOST
              valueFrom:
                configMapKeyRef:
                  name: back-customer-prod
                  key: kafka_host

---
apiVersion: v1
kind: Service
metadata:
  name: backend-customer-service-prod
spec:
  type: ClusterIP
  selector:
    app: back-customer-prod
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081