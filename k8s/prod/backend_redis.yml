apiVersion: v1
kind: PersistentVolume
metadata:
  name: redis-pv-prod
spec:
  storageClassName: standard
  capacity:
    storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data
    type: DirectoryOrCreate
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvc-prod
spec:
  volumeName: redis-pv-prod
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis7-deployment-prod
  labels:
    app: redis-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-prod
  template:
    metadata:
      labels:
        app: redis-prod
    spec:
      containers:
        - image: redis:7.2
          name: redis7
          ports:
            - containerPort: 6379
      volumes:
        - name: redis-prod
          persistentVolumeClaim:
            claimName: redis-pvc-prod

---
apiVersion: v1
kind: Service
metadata:
  name: redis7-service-prod
spec:
  type: ClusterIP
  selector:
    app: redis-prod
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379





