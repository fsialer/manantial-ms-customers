apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gateway-keycloak-ingress-staging
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: "letsencrypt-staging"
spec:
  ingressClassName: nginx
  rules:
    - host: manantial.staging.test
      http:
        paths:
        - path: /api
          pathType: Prefix
          backend:
            service:
              name: backend-gateway-service-staging
              port:
                number: 8090
    - host: manantial.staging.test
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: keycloak-staging
              port:
                number: 80
