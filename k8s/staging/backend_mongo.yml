apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongo-pv-staging
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/db
    type: DirectoryOrCreate
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-pvc-staging
spec:
  volumeName: mongo-pv-staging
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo6-deployment-staging
  labels:
    app: mongo-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-staging
  template:
    metadata:
      labels:
        app: mongo-staging
    spec:
      containers:
        - image: mongo:6.0
          name: mongo6
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mongo-secrets-staging
                  key: mongo_user
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mongo-secrets-staging
                  key: mongo_password
          volumeMounts:
            - name: mongo-staging
              mountPath: /data/db
      volumes:
        - name: mongo-staging
          persistentVolumeClaim:
            claimName: mongo-pvc-staging
---
apiVersion: v1
kind: Service
metadata:
  name: mongo6-service-staging
spec:
  type: ClusterIP
  selector:
    app: mongo-staging
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
