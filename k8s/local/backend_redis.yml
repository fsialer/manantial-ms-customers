apiVersion: v1
kind: PersistentVolume
metadata:
  name: redis-pv-local
spec:
  storageClassName: standard
  capacity:
    storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data
    type: DirectoryOrCreate
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvc-local
spec:
  volumeName: redis-pv-local
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis7-deployment
  labels:
    app: redis-local
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-local
  template:
    metadata:
      labels:
        app: redis-local
    spec:
      containers:
        - image: redis:7.2
          name: redis7
          ports:
            - containerPort: 6379
      volumes:
        - name: redis-local
          persistentVolumeClaim:
            claimName: redis-pvc-local

---
apiVersion: v1
kind: Service
metadata:
  name: redis7-service-local
spec:
  type: ClusterIP
  selector:
    app: redis-local
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379





