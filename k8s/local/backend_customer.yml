apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kubernetes.io/change-cause: "Image feralexis007/manantial-customers-service:dev-d8fee31"
  name: backend-customer-deployment-local
  labels:
    app: back-customer-local
spec:
  replicas: 3
  selector:
    matchLabels:
      app: back-customer-local
  template:
    metadata:
      labels:
        app: back-customer-local
    spec:
      containers:
        - name: customers-service
          image: feralexis007/manantial-customers-service:dev-d8fee31
          ports:
            - containerPort: 8081
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: back-customer-local
                  key: db_host
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mongo-secrets-local
                  key: mongo_user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mongo-secrets-local
                  key: mongo_password
            - name: KAFKA_HOST
              valueFrom:
                configMapKeyRef:
                  name: back-customer-local
                  key: kafka_host

---
apiVersion: v1
kind: Service
metadata:
  name: backend-customer-service-local
spec:
  type: ClusterIP
  selector:
    app: back-customer-local
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081